import 'package:flutter/material.dart';

class EntrarTorneio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Entrar em um Torneio'),
      ),
      body: Center(
        child: Text('Tela de entrada em um torneio'),
      ),
    );
  }
}
