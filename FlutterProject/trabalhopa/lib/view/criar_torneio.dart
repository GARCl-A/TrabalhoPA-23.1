import 'package:flutter/material.dart';

class CriarTorneio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Criar Torneio'),
      ),
      body: Center(
        child: Text('Tela de criação de torneio'),
      ),
    );
  }
}
